Bonjour {{ user.username }}

{{ evenement.user.username }} vient de vous valider pour l'événement 
{{ evenement.nom }} du {{ evenement.date|date('d-m-Y') }} à 
{{ evenement.heure|date('H:i')}}.

Bonne sortie.