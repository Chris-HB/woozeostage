Bonjour {{ user.username }}

{{ evenement.user.username }} vient de vous mettre en attente 
ou de vous refuser pour : {{ evenement.nom }} 
du {{ evenement.date|date('d-m-Y') }} à {{ evenement.heure|date('H:i')}}.

Ce changement peut être du a une modification de la sortie.